#include <stdio.h>

int main()
{
    float x,y;
    char c;
    if(scanf("%f%f%c", &x,&y,&c) == 3 && (float)x == x && (float)y == y && c == '\n')
    {
        if(x*x + y*y < 25)
            return printf("GOTCHA");
        
        else
            return printf("MISS");
    }
        
    else
        printf ("n/a");
    return 0;
}
